info:
  name: unlike
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/comments/{{commentId}}/like"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
