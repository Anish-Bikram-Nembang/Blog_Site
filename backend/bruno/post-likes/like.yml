info:
  name: like
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/posts/{{postId}}/like"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
