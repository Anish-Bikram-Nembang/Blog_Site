info:
  name: deleteComment
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/comments/{{commentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
