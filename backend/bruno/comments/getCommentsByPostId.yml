info:
  name: getCommentsByPostId
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/posts/{{postId}}/comments?limit=10&page=1"
  params:
    - name: limit
      value: "10"
      type: query
    - name: page
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
