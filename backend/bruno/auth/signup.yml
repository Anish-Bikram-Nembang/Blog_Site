info:
  name: signup
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/auth/signup"
  body:
    type: json
    data: |-
      {
        "username": "Enkoki2",
        "password": "cannotDefeatOrinOfWater"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
