info:
  name: getPostBySlug
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/posts/{{slug}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
